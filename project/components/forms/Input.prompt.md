Text or search input — recessed inset border, aqua focus ring.

```jsx
<Input shape="pill" placeholder="Search a spot or marina" iconLeft={<SearchIcon/>} />
<Input shape="box" placeholder="Boat length (ft)" />
```

`shape` pill | box. Sizes `sm | md | lg`. `invalid` shows the avoid-red ring. Spreads native input props.
