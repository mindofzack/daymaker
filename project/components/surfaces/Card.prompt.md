Base surface container. `media` adds a full-bleed top image slot; `hoverable` lightens + lifts for grid cards.

```jsx
<Card hoverable media={<img src="/spot.jpg" style={{width:'100%',height:'100%',objectFit:'cover'}}/>}>
  <strong>Marlin Canyon</strong>
</Card>
```
